#!/bin/bash

# Get the directory where this script is located
BIN_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Call biblecli with the passed arguments and force -s tob
# Using "$@" to pass all arguments (e.g. verse ref, other flags)
# And appending -s tob
# Note: Since biblecli logic might depend on argument order or just argparse handles it.
# argparse usually handles flags anywhere.
# Pass -s tob at the end to be safe or explicit.

"$BIN_DIR/biblecli" "$@" -b tob
