#!/bin/bash

# Get the directory where this script is located
BIN_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Call biblecli with the passed arguments and force -s bj
"$BIN_DIR/biblecli" "$@" -b bj
